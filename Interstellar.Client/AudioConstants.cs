namespace Interstellar;

/// <summary>
/// Shared audio constants — replaces the old Interstellar.Messages.AudioHelpers.
/// </summary>
internal static class AudioConstants
{
    /// <summary>Opus sample rate used throughout the system.</summary>
    public const int ClockRate = 48000;
}
